alert("BIENVENIDO A TU BASE DE DATOS");
let datos = [];
let nombre, dato, copias;
let tope = prompt("¿Cuántos datos quieres Ingresar?");

    const agregar={
        for(let i=0;i<tope;i++){

            nombre :  prompt("Nombre: "),
            dato : prompt("Dato: "),
            copias : prompt("Copias: ")

    }
}
    // agregar.nombre[i] = prompt("Nombre: ");
    // agregar.dato[i] = prompt("Dato: ");
    // agregar.copias[i] = prompt("Copias: ");   
    agregar.forEach(function(dato){
    console.log(`Objeto: ${agregar.nombre[i]}\n
                  Dato: ${agregar.dato[i]}\n
                  Copias: ${agregar.copias[i]}`);
});
 





